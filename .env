# --- Application ---
PORT=7000

# --- Database for the app (matches docker-compose + application.properties) ---
DB_HOST=db
DB_PORT=3306
DB_NAME=world
DB_USER=app
DB_PASS=app

# --- MySQL container bootstrap (used only by docker-compose) ---
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=world
MYSQL_USER=app
MYSQL_PASSWORD=app

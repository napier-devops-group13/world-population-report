# src/main/resources/application.properties

# HTTP server port (Javalin)
port=7070

# Database connection defaults for local development/tests
DB_HOST=localhost
DB_PORT=43306   # host port from compose (43306:3306)
DB_NAME=world
DB_USER=app
DB_PASS=app

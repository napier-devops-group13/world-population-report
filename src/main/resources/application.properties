# HTTP port (can be overridden by APP_PORT env var)
app.port=7070

# Database defaults (override with env vars in Docker/CI)
db.host=localhost
db.port=3306
db.name=world
db.user=root
db.password=example
